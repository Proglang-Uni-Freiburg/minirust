use std::conv;
use std::env;
use std::file;
use std::io;
use std::proc;
use std::time;


fn main() {
    let path = "/tmp/foo"

    let file = file::handle(path)

    file.write(file.read()+ "foo");

    let contents = file.read()

    io::println("Contents: " + contents)
}